# Project Instructions

## Local overrides

If `CLAUDE.local.md` exists at the repo root, import it so personal / machine-specific instructions take effect without being committed:

@CLAUDE.local.md

`CLAUDE.local.md` is gitignored. Use it for local-only context (private notes, machine paths, in-progress experiments) that should not be shared with the team.
